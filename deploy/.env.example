# docker compose 변수 치환용 (deploy/.env로 복사해 사용 — gitignore됨)
# 배포·롤백은 이 두 값을 커밋 SHA 태그로 바꾸고 `docker compose up -d`.
BACKEND_IMAGE=ghcr.io/chanyong1027/stagelog-backend:sha-REPLACE
FRONTEND_IMAGE=ghcr.io/chanyong1027/stagelog-frontend:sha-REPLACE
